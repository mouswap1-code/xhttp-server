applications:
  app:
    type: nodejs:20

routes:
  https://{default}/:
    type: proxy
    to: http://ultrategateworld.benbilal237free.xyz:80
  http://{default}/:
    type: redirect
    to: https://{default}/
